export interface Project {
  title: string
  slug: string
  excerpt: string
  description: string
  images: string[]
  stack: string[]
  repoUrl?: string
  demoUrl?: string
  category: string
  lastUpdated: string
}
